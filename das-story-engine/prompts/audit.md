# Audit Prompt (Opus)

> PLACEHOLDER — the final creative instruction text comes from a founder + Claude
> story-engine session (Phase 4 specialist task).
>
> Contract with the pipeline:
> - Step 1: call the mechanical `run_audit_checks` tool first (plants paid,
>   every scene turns, no scene left in draft).
> - Step 2: judge the creative layer on top — subtext violations,
>   cause-and-effect chain, want-vs-need gap.
> - Failing scenes go back to draft via `flag_scene` with a plain-English reason.
