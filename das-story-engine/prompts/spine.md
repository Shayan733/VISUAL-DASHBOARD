# Spine Prompt (Opus)

> PLACEHOLDER — the final creative instruction text comes from a founder + Claude
> story-engine session. This file governs the brief → 4-line-spine step.
>
> Contract with the pipeline:
> - Input: the project's `brief_text` (a replication brief from das-analyst).
> - Output: exactly 4 lines — want, stakes, hero_belief, villain_belief —
>   saved via the `save_spine` tool (never approved automatically; the founder
>   approves).
